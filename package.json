{
  "name": "capi",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "CAPI - Cloudflare API scaffold modeled after the Royal SRC endpoint documentation.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy && ./scripts/restart-bot.sh",
    "test": "node --test",
    "bot": "node src/discord-bot.js"
  },
  "dependencies": {
    "discord.js": "^14.15.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "wrangler": "^3.105.1"
  }
}
